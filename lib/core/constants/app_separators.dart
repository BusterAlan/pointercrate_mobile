import "package:flutter/material.dart";

/// App separators to use in the application
class AppSeparators {
  /// Page separator to use in the application
  static EdgeInsets get pageSeparator => const EdgeInsets.all(24);
  /// Large separator to use in the application
  static EdgeInsets get large => const EdgeInsets.all(16);
  /// Medium separator to use in the application
  static EdgeInsets get medium => const EdgeInsets.all(8);
  /// Small separator to use in the application
  static EdgeInsets get small => const EdgeInsets.all(4);
}
