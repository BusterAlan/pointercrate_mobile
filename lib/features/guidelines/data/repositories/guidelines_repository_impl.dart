import "../../business/repositories/guidelines_repository.dart";

/// Data operations for the Guidelines collection
class GuidelinesRepositoryImpl implements GuidelinesRepository {
  /// Data operations for the Guidelines collection
  GuidelinesRepositoryImpl();  
}
