import "../../business/repositories/stats_repository.dart";

/// Data operations for the Stats collection
class StatsRepositoryImpl implements StatsRepository {
  /// Data operations for the Stats collection
  StatsRepositoryImpl();
}
